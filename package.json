{
  "name": "ajv-request-validator",
  "version": "0.1.0",
  "description": "Tool for validating the `request` object of popular frameworks with Ajv",
  "main": "ajv-request-validator.js",
  "files": [
    "ajv-request-validator.js"
  ],
  "engines": {
    "node": ">=6"
  },
  "license": "MIT",
  "author": "Nathan Woltman <nwoltman@outlook.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/medleyjs/ajv-request-validator.git"
  },
  "homepage": "https://github.com/medleyjs/ajv-request-validator#readme",
  "bugs": {
    "url": "https://github.com/medleyjs/ajv-request-validator/issues"
  },
  "keywords": [
    "ajv",
    "request",
    "validation",
    "schema",
    "json-schema",
    "body",
    "fast",
    "express",
    "fastify",
    "medley"
  ],
  "dependencies": {
    "ajv": "6.x"
  },
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^5.16.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1"
  },
  "scripts": {
    "lint": "eslint *.js test/*.js",
    "test-only": "nyc --reporter=html --reporter=text-summary mocha",
    "test": "npm run lint && npm run test-only",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  }
}
